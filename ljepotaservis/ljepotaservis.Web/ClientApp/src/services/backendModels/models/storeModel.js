class StoreModel {
  constructor(name, address, openDateTime, closingDateTime) {
    this.name = name;
    this.address = address;
    this.openDateTime = openDateTime;
    this.closingDateTime = closingDateTime;
  }
}

export default StoreModel;
